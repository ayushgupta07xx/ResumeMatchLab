# PostHog product analytics (OPTIONAL — the app runs fully without it).
POSTHOG_KEY=
POSTHOG_HOST=https://us.i.posthog.com

# Only needed to REBUILD the job snapshot (scripts/export_from_jobatlas.py).
# ResumeMatch ships the snapshot pre-built, so these are not required to run.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
